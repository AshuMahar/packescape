[build]
  command = "npm run build"
  publish = "dist"

[env]
  [env.production]
    NODE_ENV = "production"

# Redirects for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
